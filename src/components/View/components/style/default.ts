import { ButtonStyle, RectStyle } from "./types"

export const DefaultRectStyle: RectStyle = {
    backGroundColor: "#ffffff",
    border: 1,
    borderColor: "#666666",
    radius: 0,
}

export const DefaultButtonStyle: ButtonStyle = {
    backGroundColor: "#3F4969",
    border: 0,
    borderColor: "#666666",
    radius: 5,
    color: "#000000",
    fontSize: 20,
    align: "center",
}
